apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-prometheus-server
  namespace: gitlab
  labels:
    app: gitlab-prometheus-server
spec:
  storageClassName: local-path
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi